# Personal Sonic Postcards

**URV5 v3 Now Live** (evolved from v2)

## URV5 Workflow v3 (Inside Both Web Apps)

**v3 (Current - Updated Tonight)**:

- Tap NFC card or open link with profile params (`?profile=juice&mode=cinematic`).
- Hit **Cinematic Preset** on any track, or tweak the comprehensive stems live in real-time:
  - Longer Vox with emotional breathy vibrato simulation
  - Complex Drum Patterns with richer LFO modulation, multi-layer textures + Fill/Chop triggers
  - Synth Melody + Ground/Strings: wider cinematic pads
  - Piano Riffs with dynamic velocity variation + Run/Cut
- Cinematic Preset intelligently balances for movie-like impact while keeping all stem controls fully editable.
- **Group Reflection Logs** with NFC ritual stamping (tap card to log reflections with timestamp, preset, and NFC serial).
- Advanced NFC ritual tracking and history modal.
- Real-time stem sync between profiles (per-profile localStorage).
- One NFC tap authorizes the entire batch + stems.

**This guidance evolves with each iteration.**

**Previous (v2)**: Batch processing queue, voice-note overlay or metadata embedding, NFC card programming (write preset ID → auto-select room), one-click exports tailored for Luna & Sonic Postcards (WAV + rich JSON), darkroom-style film-burn animation during rendering.

## Stems – More Comprehensive for Interactive Change in Feel
Enhanced modulation and layering for bigger, more noticeable shifts:

- **Longer Vox**: Emotional breathy with vibrato simulation
- **Complex Drum Patterns**: Multi-layered rhythmic textures (LFO + triggers for fills/chops)
- **Synth Melody + Ground/Strings**: Wider cinematic pads
- **Piano Riffs**: Dynamic runs with velocity variation

## Multi-Profile & NFC
`?profile=juice&mode=cinematic` supported. Different data per profile (presets, stem settings, reflection logs).

**Repo ready for auto-deploy on main.** Juice Cinema v3 is the core cinematic tool integrated into the Personal Sonic Postcards ecosystem.